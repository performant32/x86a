#include "pch.h"
#include "code_generator.h"

namespace x86a{
    std::optional<std::string> CodeGenerator::generate(const SectionContainer& section_container, std::filesystem::path output){
        return std::nullopt;
    }
}
